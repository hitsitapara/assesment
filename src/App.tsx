import HomeScreen from './components/HomeScreen';
import Header from './components/Header';

function App() {
  return (
    <>
      {/* The Header component is used to display the header of the application */}
      <Header />
      {/* The HomeScreen component is used to display the main content of the application */}
      <HomeScreen />
    </>
  );
}

export default App;
